/*{
    this.props.replies.map((reply,index) => {
      if(this.props.cId == reply.commentableId){
        return (
          <div key={index}>
            <span><strong>{reply.email}</strong></span>
            <span>{reply.comment}</span>
          </div>)
      }
    })
}*/
